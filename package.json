{
  "name": "monster-reincarnation-incremental",
  "private": true,
  "version": "1.0.0",
  "description": "Incremental/idle monster reincarnation game — free web demo + Cloudflare backend.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @mri/client",
    "build": "npm run build -w @mri/client",
    "preview": "npm run preview -w @mri/client",
    "server:dev": "npm run dev -w @mri/server",
    "server:deploy": "npm run deploy -w @mri/server",
    "typecheck": "tsc -p packages/shared && tsc -p packages/client && tsc -p packages/server"
  },
  "license": "UNLICENSED"
}
