# Tutorial Sistemi Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Oyuna katmanlı, atlanabilir, her zaman erişilebilir bir tutorial sistemi ekle: 8 adımlı sihirbaz, 10 bağlamsal hint toast, 12 bölümlü Kılavuz sekmesi, ? navigasyon ikonları ve race seçim ekranına ırk açıklaması paneli.

**Architecture:** `tutorial.ts` adım/hint tanımlarını barındırır; `GameState`'e `tutorialStep` ve `seenHints` eklenir; `ui.ts`'e `'guide'` sekmesi, wizard overlay ve hint mekanizması entegre edilir. Tüm metin i18n anahtarı üzerinden gelir (tr/en/ru). `race_hints.json` ırk açıklamasını tutar — `races.json`'a dokunulmaz.

**Tech Stack:** TypeScript, Vite, vanilla DOM, mevcut `t()` / `pushToast()` / `UiActions` pattern'leri.

## Global Constraints

- Oyuncuya görünen hiçbir metin koda gömülmez — her string `t('anahtar')` üzerinden gelir.
- `evolutions.json`, `races.json`, `evolution.ts` dosyalarına kesinlikle dokunulmaz.
- `D:\MyAi` (Marvion) bu session'dan değiştirilemez.
- Build komutu: `npm run build` (`packages/client/` altında Vite). Her task sonunda çalıştırılır.
- Yeni TypeScript tipler `@mri/shared` paketine değil, ilgili dosyaya eklenir.
- State değişiklikleri `migrate()` fonksiyonunda backfill edilir (mevcut kayıtları bozmaz).

---

## Dosya Haritası

| İşlem | Dosya | Ne değişir |
|-------|-------|-----------|
| Oluştur | `packages/client/src/tutorial.ts` | Adım/hint tanımları + yardımcı fonksiyonlar |
| Oluştur | `data/race_hints.json` | Irk açıklaması verisi |
| Değiştir | `packages/client/src/game/state.ts` | `tutorialStep`, `seenHints` alanları |
| Değiştir | `packages/client/src/main.ts` | migrate() + UiActions handler'ları |
| Değiştir | `packages/client/src/ui.ts` | Guide sekmesi, wizard overlay, hint sistemi, ? ikonları, race hint paneli |
| Değiştir | `data/i18n/tr.json` | Tutorial/guide/hint anahtarları |
| Değiştir | `data/i18n/en.json` | Tutorial/guide/hint anahtarları |
| Değiştir | `data/i18n/ru.json` | Tutorial/guide/hint anahtarları |

---

## Task 1: State Alanları + Migration

**Files:**
- Modify: `packages/client/src/game/state.ts`
- Modify: `packages/client/src/main.ts`

**Interfaces:**
- Produces: `GameState.tutorialStep: number | 'done' | 'skipped'`, `GameState.seenHints: string[]`

- [ ] **Step 1: `state.ts` — GameState interface'e alanları ekle**

`GameState` interface'inde `pendingForage: ...` satırının hemen altına ekle:

```typescript
  /** Tutorial sihirbazı mevcut adım. 0 = henüz başlamadı, 'done' = tamamlandı, 'skipped' = atlandı. */
  tutorialStep: number | 'done' | 'skipped';
  /** Gösterilmiş hint ID'leri — aynı hint tekrar çıkmaz. */
  seenHints: string[];
```

- [ ] **Step 2: `state.ts` — newGame() başlangıç değerleri**

`newGame()` fonksiyonundaki state objesi içinde `pendingForage: null,` satırının hemen altına ekle:

```typescript
    tutorialStep: 0,
    seenHints: [],
```

- [ ] **Step 3: `main.ts` — migrate() fonksiyonuna backfill**

`migrate()` fonksiyonunun sonuna, `s.pendingForage ??= null;` satırının hemen altına ekle:

```typescript
  // v10 fields — tutorial sistemi
  s.tutorialStep ??= 0;
  s.seenHints ??= [];
```

- [ ] **Step 4: Build kontrolü**

```bash
cd D:\MonsterReincarnationIncremental && npm run build
```

Beklenen: `✓ built in ...ms` — TypeScript hatası olmamalı.

- [ ] **Step 5: Commit**

```bash
git add packages/client/src/game/state.ts packages/client/src/main.ts
git commit -m "feat: tutorial state alanları — tutorialStep + seenHints"
```

---

## Task 2: tutorial.ts — Adım ve Hint Tanımları

**Files:**
- Create: `packages/client/src/tutorial.ts`

**Interfaces:**
- Produces: `TutorialStep` interface, `TUTORIAL_STEPS: TutorialStep[]`, `HINT_DEFS: HintDef[]`, `shouldShowHint(id, state): boolean`, `markHintSeen(id, state): void`

- [ ] **Step 1: Dosyayı oluştur**

`packages/client/src/tutorial.ts`:

```typescript
import type { GameState } from './game/state';

export interface TutorialStep {
  id: string;
  /** i18n key prefix — {prefix}.title ve {prefix}.body olarak okunur */
  i18nPrefix: string;
  /** Hedef sekme; wizard 'Götür' butonuna tıklanınca bu sekme açılır */
  targetTab: string | null;
  /** DOM selector — sekme açıldıktan sonra scroll/highlight edilecek element */
  targetSelector: string | null;
}

export interface HintDef {
  id: string;
  i18nPrefix: string;
  targetTab: string;
  /** Kılavuz sekmesi anchor'ı — hint toast'taki 'Götür' linki buraya gider */
  guideAnchor: string;
  /** Bu fonksiyon true döndürdüğünde hint tetiklenir */
  trigger: (state: GameState) => boolean;
}

export const TUTORIAL_STEPS: TutorialStep[] = [
  { id: 'welcome',    i18nPrefix: 'tut.step1', targetTab: null,        targetSelector: null },
  { id: 'bars',       i18nPrefix: 'tut.step2', targetTab: null,        targetSelector: '#topbar' },
  { id: 'combat',     i18nPrefix: 'tut.step3', targetTab: 'combat',    targetSelector: null },
  { id: 'skills',     i18nPrefix: 'tut.step4', targetTab: 'skills',    targetSelector: null },
  { id: 'map',        i18nPrefix: 'tut.step5', targetTab: 'map',       targetSelector: null },
  { id: 'hunger',     i18nPrefix: 'tut.step6', targetTab: null,        targetSelector: '#tb-hunger-f' },
  { id: 'stats',      i18nPrefix: 'tut.step7', targetTab: 'stats',     targetSelector: null },
  { id: 'evolution',  i18nPrefix: 'tut.step8', targetTab: 'stats',     targetSelector: '.evo-tree' },
];

export const HINT_DEFS: HintDef[] = [
  {
    id: 'skill_levelup',
    i18nPrefix: 'hint.skill_levelup',
    targetTab: 'skills',
    guideAnchor: 'guide-skills',
    trigger: (s) => s.skills.some((sk) => sk.level >= 2),
  },
  {
    id: 'evo_available',
    i18nPrefix: 'hint.evo_available',
    targetTab: 'stats',
    guideAnchor: 'guide-skills',
    trigger: (s) => s.seenForms.length > 0 || s.formHistory.length > 1,
  },
  {
    id: 'stat_point',
    i18nPrefix: 'hint.stat_point',
    targetTab: 'stats',
    guideAnchor: 'guide-stats',
    trigger: (s) => s.statPoints > 0,
  },
  {
    id: 'fusion_unlock',
    i18nPrefix: 'hint.fusion_unlock',
    targetTab: 'skills',
    guideAnchor: 'guide-fusion',
    trigger: (s) => s.fusionUnlocked,
  },
  {
    id: 'meditation_unlock',
    i18nPrefix: 'hint.meditation_unlock',
    targetTab: 'stats',
    guideAnchor: 'guide-soul',
    trigger: (s) => s.meditationUnlocked,
  },
  {
    id: 'soul_tree',
    i18nPrefix: 'hint.soul_tree',
    targetTab: 'stats',
    guideAnchor: 'guide-soul',
    trigger: (s) => s.souls > 0,
  },
  {
    id: 'human_path',
    i18nPrefix: 'hint.human_path',
    targetTab: 'stats',
    guideAnchor: 'guide-human_path',
    trigger: (s) => s.pendingHumanPath || s.humanPath !== undefined,
  },
  {
    id: 'first_item',
    i18nPrefix: 'hint.first_item',
    targetTab: 'inventory',
    guideAnchor: 'guide-inventory',
    trigger: (s) => s.inventoryItems.length > 0,
  },
  {
    id: 'first_bestiary',
    i18nPrefix: 'hint.first_bestiary',
    targetTab: 'bestiary',
    guideAnchor: 'guide-lore',
    trigger: (s) => Object.keys(s.killedEnemies).length > 0,
  },
  {
    id: 'hunger_warning',
    i18nPrefix: 'hint.hunger_warning',
    targetTab: null,
    guideAnchor: 'guide-hunger',
    trigger: (s) => s.hunger > 0 && s.hunger / 1000 > 0.7,
  },
];

/** Hint gösterilmeli mi? seenHints'e bakılır + trigger koşulu kontrol edilir. */
export function shouldShowHint(id: string, state: GameState): boolean {
  if (state.seenHints.includes(id)) return false;
  const def = HINT_DEFS.find((h) => h.id === id);
  return def ? def.trigger(state) : false;
}

/** Hint'i gösterildi olarak işaretle (state'e yazar, save çağırmaz). */
export function markHintSeen(id: string, state: GameState): void {
  if (!state.seenHints.includes(id)) state.seenHints.push(id);
}
```

- [ ] **Step 2: Build kontrolü**

```bash
cd D:\MonsterReincarnationIncremental && npm run build
```

Beklenen: `✓ built in ...ms`

- [ ] **Step 3: Commit**

```bash
git add packages/client/src/tutorial.ts
git commit -m "feat: tutorial.ts — adım ve hint tanımları"
```

---

## Task 3: race_hints.json

**Files:**
- Create: `data/race_hints.json`

**Interfaces:**
- Produces: `RaceHint` veri yapısı — `{ [raceId]: { tr: {strength,weakness,growth,note}, en: {...}, ru: {...} } }`

- [ ] **Step 1: Dosyayı oluştur**

`data/race_hints.json`:

```json
{
  "spider": {
    "tr": {
      "strength": "Zehir + ipek kombinasyonu, erken oyunda güçlü sürekli hasar",
      "weakness": "Düşük başlangıç HP — dikkatli oynamak gerekir",
      "growth": "Hatchling Spider → Cave Spider → Poison Weaver / Web Hunter / ...",
      "note": "Fusion sistemi erken açılır; skill kombinasyonları zengindir"
    },
    "en": {
      "strength": "Venom + silk combo deals strong sustained damage early",
      "weakness": "Low starting HP — requires careful play",
      "growth": "Hatchling Spider → Cave Spider → Poison Weaver / Web Hunter / ...",
      "note": "Fusion unlocks early; rich skill combination options"
    },
    "ru": {
      "strength": "Комбинация яда и шёлка даёт стабильный урон с первых минут",
      "weakness": "Низкий начальный HP — требует аккуратной игры",
      "growth": "Hatchling Spider → Cave Spider → Poison Weaver / Web Hunter / ...",
      "note": "Fusion открывается рано; богатые комбинации навыков"
    }
  },
  "slime": {
    "tr": {
      "strength": "Yüksek dayanıklılık ve absorbsiyon yeteneği",
      "weakness": "Saldırı gücü başlangıçta düşük",
      "growth": "Slime → Poison Slime / Armored Slime → ...",
      "note": "Resistance odaklı evrim ağacı"
    },
    "en": {
      "strength": "High durability and absorption ability",
      "weakness": "Low attack power at the start",
      "growth": "Slime → Poison Slime / Armored Slime → ...",
      "note": "Resistance-focused evolution tree"
    },
    "ru": {
      "strength": "Высокая выносливость и способность к поглощению",
      "weakness": "Низкая атака в начале игры",
      "growth": "Slime → Poison Slime / Armored Slime → ...",
      "note": "Дерево эволюции сфокусировано на сопротивлениях"
    }
  },
  "skeleton": {
    "tr": {
      "strength": "Yüksek fiziksel saldırı, erken silah uzmanlığı",
      "weakness": "Isı/ışık hasarına karşı savunmasız",
      "growth": "Skeleton → Skeleton Warrior → Death Knight / Lich → ...",
      "note": "Ölüm enerjisi mekanikleri ile benzersiz oynanış"
    },
    "en": {
      "strength": "High physical attack, early weapon specialization",
      "weakness": "Vulnerable to heat/light damage",
      "growth": "Skeleton → Skeleton Warrior → Death Knight / Lich → ...",
      "note": "Unique gameplay with death energy mechanics"
    },
    "ru": {
      "strength": "Высокая физическая атака, ранняя специализация оружия",
      "weakness": "Уязвимость к тепловому/световому урону",
      "growth": "Skeleton → Skeleton Warrior → Death Knight / Lich → ...",
      "note": "Уникальный геймплей с механиками энергии смерти"
    }
  },
  "golem": {
    "tr": {
      "strength": "En yüksek başlangıç savunması, yüksek HP",
      "weakness": "Yavaş hareket, düşük AGI",
      "growth": "Stone Golem → Iron Golem / Magma Golem → ...",
      "note": "Tank oynanışı — hasar almak skill exp kazandırır"
    },
    "en": {
      "strength": "Highest starting defense, high HP",
      "weakness": "Slow movement, low AGI",
      "growth": "Stone Golem → Iron Golem / Magma Golem → ...",
      "note": "Tank playstyle — taking damage also earns skill exp"
    },
    "ru": {
      "strength": "Наибольшая начальная защита и HP",
      "weakness": "Медленное передвижение, низкий AGI",
      "growth": "Stone Golem → Iron Golem / Magma Golem → ...",
      "note": "Стиль танка — получение урона тоже даёт опыт навыков"
    }
  },
  "human": {
    "tr": {
      "strength": "Dengeli başlangıç statları, geniş evrim seçenekleri",
      "weakness": "Başlangıçta özel avantajı yok",
      "growth": "Human → Swordsman / Mage / Ranger → ...",
      "note": "İnsan Yolu sistemiyle benzersiz uzmanlaşma dalları"
    },
    "en": {
      "strength": "Balanced starting stats, wide evolution options",
      "weakness": "No special starting advantage",
      "growth": "Human → Swordsman / Mage / Ranger → ...",
      "note": "Unique specialization branches via the Human Path system"
    },
    "ru": {
      "strength": "Сбалансированные начальные характеристики, широкие варианты эволюции",
      "weakness": "Нет особых стартовых преимуществ",
      "growth": "Human → Swordsman / Mage / Ranger → ...",
      "note": "Уникальные ветки специализации через систему Пути Человека"
    }
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add data/race_hints.json
git commit -m "feat: race_hints.json — ırk açıklama verisi (tr/en/ru)"
```

---

## Task 4: i18n Anahtarları (tr / en / ru)

**Files:**
- Modify: `data/i18n/tr.json`
- Modify: `data/i18n/en.json`
- Modify: `data/i18n/ru.json`

**Not:** Her üç dosyada da aynı anahtar seti eklenir; sadece değerler farklıdır. Anahtarlar dosyanın sonuna, son `}` kapanışından önce eklenir.

- [ ] **Step 1: `en.json`'a eklenecek anahtarları belirle**

Dosyanın sonundaki son anahtardan sonra (son `}` öncesi) virgül + aşağıdaki blok eklenir:

```json
,
  "tab.guide": "Guide",
  "tut.next": "Next",
  "tut.skip_all": "Skip All",
  "tut.goto": "Go There",
  "tut.done": "Done!",
  "tut.reopen": "Open Tutorial",
  "tut.reopen_greeting": "Look who's back 👀",
  "tut.step": "Step",
  "tut.of": "of",
  "tut.step1.title": "Welcome, Adventurer!",
  "tut.step1.body": "In this world, Knowledge = Power. You start as a weak creature — but through combat, skills, and evolution you will become unstoppable. This tutorial will guide you through the basics.",
  "tut.step2.title": "Your Status Bars",
  "tut.step2.body": "At the top you can see three bars: HP (health — if it hits 0 you die), MP (mana — used by skills), and SP (stamina — used by special abilities). Hunger also appears here — keep it in check!",
  "tut.step3.title": "Combat",
  "tut.step3.body": "In the Combat tab you can attack enemies. Use Auto mode to fight automatically, or Manual mode to trigger skills yourself. Defeating enemies earns XP and skill experience.",
  "tut.step4.title": "Skills",
  "tut.step4.body": "Every skill gains experience as you use it. At level 10, a skill can evolve into a more powerful form. Check the Skills tab to see your skills and their progress.",
  "tut.step5.title": "Map & Rooms",
  "tut.step5.body": "The dungeon is structured as Layer.Floor.Room. Clear rooms to advance, reach the boss to unlock the next floor. Use the Map tab to navigate floors you've already cleared.",
  "tut.step6.title": "Hunger",
  "tut.step6.body": "Hunger rises over time. If you get too hungry, your stats will drop. Use the Forage button to find food, or keep defeated enemies in your larder to eat later.",
  "tut.step7.title": "Stat Points",
  "tut.step7.body": "Each time you level up, you earn Stat Points. Spend them in the Stats tab to strengthen STR (attack), VIT (HP), AGI (speed/dodge), INT (MP/skills), WIS (mana regen), or LUCK.",
  "tut.step8.title": "Evolution",
  "tut.step8.body": "When you meet the requirements, your race can evolve into a new form. Each form has different bonuses and unlocks new evolution branches. Check the evolution tree in the Stats tab!",
  "hint.skill_levelup.title": "Skill Level Up!",
  "hint.skill_levelup.body": "One of your skills gained a level. At level 10 it can evolve into a more powerful form.",
  "hint.evo_available.title": "Evolution Available!",
  "hint.evo_available.body": "Your race can now evolve. Open the Stats tab to see the evolution tree.",
  "hint.stat_point.title": "Stat Point Earned!",
  "hint.stat_point.body": "You earned a Stat Point. Spend it in the Stats tab to strengthen your character.",
  "hint.fusion_unlock.title": "Fusion Unlocked!",
  "hint.fusion_unlock.body": "You can now combine two skills to create a new one. Find the Fusion option in the Skills tab.",
  "hint.meditation_unlock.title": "Meditation Unlocked!",
  "hint.meditation_unlock.body": "You can now meditate to gain Virtue and recover. Find it in the Stats tab.",
  "hint.soul_tree.title": "Soul Tree Accessible!",
  "hint.soul_tree.body": "You have Souls to spend. Open the Soul Tree in the Stats tab to unlock permanent upgrades.",
  "hint.human_path.title": "Human Path!",
  "hint.human_path.body": "A critical choice awaits — your Human Path specialization. Check the Stats tab to decide.",
  "hint.first_item.title": "Item Found!",
  "hint.first_item.body": "You found an item. Open the Inventory tab to equip or discard it.",
  "hint.first_bestiary.title": "Bestiary Updated!",
  "hint.first_bestiary.body": "Your first enemy entry was recorded. Check the Bestiary tab to study your foes.",
  "hint.hunger_warning.title": "Hunger Warning!",
  "hint.hunger_warning.body": "Your hunger is getting critical. Use Forage to find food before your stats drop.",
  "hint.goto": "Go",
  "guide.title": "Guide",
  "guide.basics.title": "Basic Status Bars",
  "guide.basics.body": "HP (green bar): your health. If it reaches 0, you die and restart. MP (blue bar): mana, spent when you use skills. SP (gold bar): stamina, used by special abilities. Hunger (brown bar): rises over time — high hunger lowers your stats.",
  "guide.combat.title": "Combat System",
  "guide.combat.body": "In the Combat tab you fight enemies. Auto mode attacks automatically each second. Manual mode lets you choose when to use each skill. Defeating enemies earns XP (levels you up) and skill EXP (levels up individual skills). Your Attack Cooldown (shown in combat) determines how fast you strike.",
  "guide.skills.title": "Skills & Evolution",
  "guide.skills.body": "Each skill gains EXP when used in combat. At level 10, a skill may evolve into an upgraded form with new effects. Some evolutions branch — choose carefully. The Skill Tree shows chains and unlock conditions. Equip skills from your loadout to use them in combat (up to your skill slot limit).",
  "guide.map.title": "Map & Rooms",
  "guide.map.body": "The dungeon is structured as Layer → Floor → Room (shown as L.F.R at the top). Clear all rooms on a floor, then defeat the boss to advance to the next floor. Use the Map tab to revisit already-cleared floors for farming. Each layer has a tier requirement.",
  "guide.hunger.title": "Hunger & Forage",
  "guide.hunger.body": "Hunger rises as time passes during combat or rest. When hunger passes 70%, your stats start to drop. Use the Forage button (in the Combat tab) to search for food. You can also store enemy corpses in your larder and eat them later. Auto-Eat will eat automatically when hunger gets high.",
  "guide.stats.title": "Stats & Stat Points",
  "guide.stats.body": "You have 6 core stats: STR (physical attack power), VIT (max HP), AGI (attack speed and dodge chance), INT (max MP and skill effectiveness), WIS (MP regen rate), LUCK (loot and crit chance). Every level up grants Stat Points. Spend them wisely — Respec costs EP.",
  "guide.inventory.title": "Inventory & Equipment",
  "guide.inventory.body": "Available for humanoid races. Items you find go to your inventory. You can equip them to gain stat bonuses. Use Forge to upgrade rarity (costs EP). Scrap Common items to recover EP. Auto-Equip automatically equips the best items for your build.",
  "guide.soul.title": "Soul Tree & Meditation",
  "guide.soul.body": "After gaining Souls (from kills with high Sin), you can spend them in the Soul Tree for permanent upgrades that persist through rebirth. Meditation increases your Virtue stat over time and unlocks Light-aligned ruler powers. Both are found in the Stats tab.",
  "guide.fusion.title": "Skill Fusion",
  "guide.fusion.body": "Once Fusion is unlocked, you can combine two skills to create a new hybrid skill. Select two skills in the Skills tab and press Fuse. The result is determined by the combination — experiment to discover new skills! Fusion results are shared globally (same combo = same result for everyone).",
  "guide.human_path.title": "Human Path",
  "guide.human_path.body": "If your race is Human (or reaches a humanoid evolution), a specialization choice called the Human Path becomes available. This is a one-time decision that shapes your character's direction. Choose wisely — it cannot be undone without rebirth.",
  "guide.lore.title": "Bestiary & Lore",
  "guide.lore.body": "The Bestiary records every enemy you've encountered, showing their stats and weaknesses after you've fought them enough times. The Lore tab contains books you've found in the dungeon — reading them grants EP and INT bonuses. The Lore log on the bottom right preserves all book entries.",
  "guide.races.title": "Races",
  "guide.races.body": "Your race determines your starting skills, stats, and evolution tree. You can change your race until your first kill. Each race has unique strengths and growth paths — see the cards below.",
  "race_hint.strength": "Strength",
  "race_hint.weakness": "Weakness",
  "race_hint.growth": "Growth Path",
  "race_hint.note": "Note"
```

- [ ] **Step 2: `en.json`'u düzenle**

`data/i18n/en.json` dosyasını aç. Son satır `}` şeklindedir. O `}` öncesine yukarıdaki bloku ekle (dosyanın son anahtarından sonra virgül var olduğundan emin ol).

- [ ] **Step 3: `tr.json`'a Türkçe değerleri ekle**

Aynı anahtar setini Türkçe değerlerle `data/i18n/tr.json` sonuna ekle:

```json
,
  "tab.guide": "Kılavuz",
  "tut.next": "İleri",
  "tut.skip_all": "Tümünü Atla",
  "tut.goto": "Götür",
  "tut.done": "Bitti!",
  "tut.reopen": "Tutorial'ı Aç",
  "tut.reopen_greeting": "Bak kim gelmiş 👀",
  "tut.step": "Adım",
  "tut.of": "/",
  "tut.step1.title": "Hoş Geldin, Maceracı!",
  "tut.step1.body": "Bu dünyada Bilgi = Güç. Zayıf bir yaratık olarak başlıyorsun — ama savaş, skiller ve evrim sayesinde güçleneceksin. Bu tutorial sana temel mekanikleri öğretecek.",
  "tut.step2.title": "Durum Barların",
  "tut.step2.body": "Üstte üç bar görüyorsun: HP (can — sıfıra düşerse ölürsün), MP (mana — skill kullanımı için), SP (stamina — özel yetenekler için). Açlık da burada görünür — dikkat et!",
  "tut.step3.title": "Savaş",
  "tut.step3.body": "Savaş sekmesinde düşmanlara saldırırsın. Otomatik modda her saniye otomatik saldırı yapılır; Manuel modda skilleri kendin tetiklersin. Düşman öldürmek XP ve skill deneyimi kazandırır.",
  "tut.step4.title": "Skiller",
  "tut.step4.body": "Her skill kullandıkça deneyim kazanır. 10. seviyede bir skill daha güçlü bir forma evrilebilir. Skill sekmesinden skillerini ve ilerlemelerini takip edebilirsin.",
  "tut.step5.title": "Harita & Odalar",
  "tut.step5.body": "Zindan Katman.Kat.Oda yapısındadır. Odaları temizleyerek ilerlenir, boss'u geçince sonraki kat açılır. Daha önce temizlediğin katları Harita sekmesinden dolaşabilirsin.",
  "tut.step6.title": "Açlık",
  "tut.step6.body": "Açlık zamanla artar. Çok acıkırsan statların düşer. Yemek Ara butonuyla yiyecek bulabilir ya da öldürdüğün düşmanları larder'da saklayıp yiyebilirsin.",
  "tut.step7.title": "Stat Puanları",
  "tut.step7.body": "Her seviye atlayışında Stat Puanı kazanırsın. Stats sekmesinden harcayabilirsin: STR (saldırı), VIT (HP), AGI (hız/dodge), INT (MP/skill), WIS (mana rejeneri), LUCK.",
  "tut.step8.title": "Evrim",
  "tut.step8.body": "Koşulları sağladığında ırkın yeni bir forma evrilebilir. Her form farklı bonuslar verir ve yeni evrim dalları açar. Stats sekmesindeki evrim ağacını kontrol et!",
  "hint.skill_levelup.title": "Skill Seviye Atladı!",
  "hint.skill_levelup.body": "Bir skillin seviye atladı. 10. seviyede daha güçlü bir forma evrilebilir.",
  "hint.evo_available.title": "Evrim Mevcut!",
  "hint.evo_available.body": "Irkın artık evrilebilir. Stats sekmesinden evrim ağacını kontrol et.",
  "hint.stat_point.title": "Stat Puanı Kazandın!",
  "hint.stat_point.body": "Bir Stat Puanı kazandın. Stats sekmesinden karakterini güçlendirmek için harca.",
  "hint.fusion_unlock.title": "Fusion Açıldı!",
  "hint.fusion_unlock.body": "Artık iki skili birleştirerek yeni bir skill oluşturabilirsin. Skill sekmesindeki Fusion seçeneğini kullan.",
  "hint.meditation_unlock.title": "Meditasyon Açıldı!",
  "hint.meditation_unlock.body": "Artık meditasyon yapabilirsin. Stats sekmesinden erişebilirsin.",
  "hint.soul_tree.title": "Ruh Ağacı Erişildi!",
  "hint.soul_tree.body": "Harcayacak Ruhun var. Stats sekmesindeki Ruh Ağacından kalıcı yükseltmeler al.",
  "hint.human_path.title": "İnsan Yolu!",
  "hint.human_path.body": "Kritik bir seçim seni bekliyor — İnsan Yolu uzmanlaşması. Karar vermek için Stats sekmesini aç.",
  "hint.first_item.title": "Eşya Bulundu!",
  "hint.first_item.body": "Bir eşya buldun. Kuşanmak veya atmak için Envanter sekmesini aç.",
  "hint.first_bestiary.title": "Bestiary Güncellendi!",
  "hint.first_bestiary.body": "İlk düşman kaydın oluştu. Düşmanlarını incelemek için Bestiary sekmesine bak.",
  "hint.hunger_warning.title": "Açlık Uyarısı!",
  "hint.hunger_warning.body": "Açlığın kritik seviyeye yaklaşıyor. Statların düşmeden önce Yemek Ara ile yiyecek bul.",
  "hint.goto": "Götür",
  "guide.title": "Kılavuz",
  "guide.basics.title": "Durum Barları",
  "guide.basics.body": "HP (yeşil): canın, sıfıra düşerse ölürsün. MP (mavi): mana, skill kullanımı için harcanır. SP (altın): stamina, özel yetenekler için. Açlık (kahverengi): zamanla artar — yüksek açlık statlarını düşürür.",
  "guide.combat.title": "Savaş Sistemi",
  "guide.combat.body": "Savaş sekmesinde düşmanlarla savaşırsın. Otomatik mod her saniye otomatik saldırır. Manuel mod her skili kendin tetiklemeni sağlar. Öldürülen düşmanlar XP (seviye) ve skill EXP (skill seviyesi) verir. Saldırı gecikmesi ne kadar hızlı vurduğunu belirler.",
  "guide.skills.title": "Skiller & Evrim",
  "guide.skills.body": "Her skill savaşta kullanıldıkça EXP kazanır. 10. seviyede skill daha güçlü bir forma evrilebilir; bazı evrimler dallanır — dikkatli seç. Skill Ağacı zincirleri ve kilit koşullarını gösterir. Skili kuşanman gerekir (loadout'a ekle) — slot sayın sınırlıdır.",
  "guide.map.title": "Harita & Odalar",
  "guide.map.body": "Zindan Katman → Kat → Oda (L.F.R) şeklindedir. Bir kattaki tüm odaları temizle, boss'u geç, sonraki kat açılır. Harita sekmesinden temizlenmiş katlara geri dönebilirsin. Her katmanın tier gereksinimi vardır.",
  "guide.hunger.title": "Açlık & Yiyecek",
  "guide.hunger.body": "Savaş veya dinlenme sırasında açlık zamanla artar. %70'i geçince statlar düşmeye başlar. Savaş sekmesindeki Yemek Ara butonuyla yiyecek ara. Düşman cesetlerini larder'da saklayıp yiyebilirsin. Oto-Ye açıksa açlık artınca otomatik yer.",
  "guide.stats.title": "Statlar & Stat Puanları",
  "guide.stats.body": "6 temel stat: STR (fiziksel saldırı), VIT (max HP), AGI (hız ve dodge şansı), INT (max MP ve skill etkinliği), WIS (MP rejeneri), LUCK (ganmet ve kritik şansı). Her seviyede Stat Puanı kazanılır. Dikkatli harca — Respec EP gerektirir.",
  "guide.inventory.title": "Envanter & Ekipman",
  "guide.inventory.body": "İnsansı ırklar için kullanılabilir. Bulunan eşyalar envantera gider. Kuşanmak için dokunabilirsin. Forge ile nadirliği yükseltebilirsin (EP harcar). Common eşyaları hurdalayarak EP kazan. Oto-Kuşan en iyi eşyaları otomatik kuşanır.",
  "guide.soul.title": "Ruh Ağacı & Meditasyon",
  "guide.soul.body": "Öldürmelerden (yüksek Günah ile) Ruh kazanılır ve Ruh Ağacında kalıcı yükseltmelere harcanır — rebirth'ten geçer. Meditasyon zamanla Erdem statını artırır ve Işık ruler güçlerini açar. İkisi de Stats sekmesindedir.",
  "guide.fusion.title": "Skill Füzyonu",
  "guide.fusion.body": "Fusion açıldıktan sonra iki skili birleştirip yeni bir hibrit skill oluşturabilirsin. Skill sekmesinde iki skill seç ve Fuse'a bas. Sonuç kombinasyona göre belirlenir — keşfetmek için dene! Füzyon sonuçları tüm oyuncular için ortaktır.",
  "guide.human_path.title": "İnsan Yolu",
  "guide.human_path.body": "Irk Human (veya insansı evrime ulaştıysa) İnsan Yolu adında uzmanlaşma seçimi açılır. Bu tek seferlik bir karardır. Dikkatli seç — rebirth olmadan geri alınamaz.",
  "guide.lore.title": "Bestiary & Lore",
  "guide.lore.body": "Bestiary karşılaştığın her düşmanı kaydeder; yeterince savaştıktan sonra stat ve zayıflıkları görünür. Lore sekmesi zindan içinde bulduğun kitapları içerir — okumak EP ve INT bonusu verir. Sağ alttaki Lore logu tüm kitap kayıtlarını saklar.",
  "guide.races.title": "Irklar",
  "guide.races.body": "Irk başlangıç skillini, statlarını ve evrim ağacını belirler. İlk öldürmeye kadar ırkını değiştirebilirsin. Her ırkın kendine özgü güçlü yanları ve gelişim yolları vardır — aşağıdaki kartlara bak.",
  "race_hint.strength": "Güçlü Yön",
  "race_hint.weakness": "Zayıf Yön",
  "race_hint.growth": "Gelişim Yolu",
  "race_hint.note": "Not"
```

- [ ] **Step 4: `ru.json`'a Rusça değerleri ekle**

Aynı anahtar setini Rusça değerlerle `data/i18n/ru.json` sonuna ekle:

```json
,
  "tab.guide": "Руководство",
  "tut.next": "Далее",
  "tut.skip_all": "Пропустить всё",
  "tut.goto": "Перейти",
  "tut.done": "Готово!",
  "tut.reopen": "Открыть туториал",
  "tut.reopen_greeting": "Смотрите кто вернулся 👀",
  "tut.step": "Шаг",
  "tut.of": "из",
  "tut.step1.title": "Добро пожаловать, Искатель!",
  "tut.step1.body": "В этом мире Знание = Сила. Ты начинаешь как слабое существо — но через бой, навыки и эволюцию станешь непобедимым. Этот туториал проведёт тебя через основы.",
  "tut.step2.title": "Шкалы состояния",
  "tut.step2.body": "Вверху три шкалы: HP (здоровье — при 0 ты умираешь), MP (мана — для навыков), SP (выносливость — для особых способностей). Также отображается голод — следи за ним!",
  "tut.step3.title": "Бой",
  "tut.step3.body": "На вкладке Бой ты сражаешься с врагами. Авто-режим атакует каждую секунду автоматически; Ручной режим позволяет самому активировать навыки. Убийство врагов даёт XP и опыт навыков.",
  "tut.step4.title": "Навыки",
  "tut.step4.body": "Каждый навык накапливает опыт при использовании. На 10-м уровне навык может эволюционировать в более мощную форму. Следи за навыками и прогрессом на вкладке Навыки.",
  "tut.step5.title": "Карта и Комнаты",
  "tut.step5.body": "Подземелье делится на Слой.Этаж.Комнату. Зачисти комнаты — победи босса — откроется следующий этаж. На вкладке Карта можно вернуться на уже пройденные этажи.",
  "tut.step6.title": "Голод",
  "tut.step6.body": "Голод растёт со временем. Если станет слишком высоким — характеристики упадут. Используй кнопку Поиск Еды для поиска пропитания или храни трупы врагов в кладовой.",
  "tut.step7.title": "Очки характеристик",
  "tut.step7.body": "При повышении уровня получаешь Очки характеристик. Трать их на вкладке Статы: STR (атака), VIT (HP), AGI (скорость/уклонение), INT (MP/навыки), WIS (восстановление маны), LUCK.",
  "tut.step8.title": "Эволюция",
  "tut.step8.body": "При выполнении условий раса может эволюционировать в новую форму. Каждая форма даёт разные бонусы и открывает новые ветки. Проверь дерево эволюции на вкладке Статы!",
  "hint.skill_levelup.title": "Навык повысился!",
  "hint.skill_levelup.body": "Один из навыков повысил уровень. На 10-м уровне он может эволюционировать.",
  "hint.evo_available.title": "Эволюция доступна!",
  "hint.evo_available.body": "Твоя раса может эволюционировать. Открой вкладку Статы и посмотри дерево эволюции.",
  "hint.stat_point.title": "Очко характеристик!",
  "hint.stat_point.body": "Получено очко характеристики. Потрать его на вкладке Статы.",
  "hint.fusion_unlock.title": "Слияние разблокировано!",
  "hint.fusion_unlock.body": "Теперь можно комбинировать два навыка для создания нового. Найди Слияние на вкладке Навыки.",
  "hint.meditation_unlock.title": "Медитация разблокирована!",
  "hint.meditation_unlock.body": "Теперь доступна медитация. Найди её на вкладке Статы.",
  "hint.soul_tree.title": "Доступно Дерево Душ!",
  "hint.soul_tree.body": "Есть Души для трат. Открой Дерево Душ на вкладке Статы для постоянных улучшений.",
  "hint.human_path.title": "Путь Человека!",
  "hint.human_path.body": "Тебя ждёт важный выбор — специализация Пути Человека. Открой вкладку Статы.",
  "hint.first_item.title": "Найден предмет!",
  "hint.first_item.body": "Ты нашёл предмет. Открой вкладку Инвентарь, чтобы экипировать или выбросить его.",
  "hint.first_bestiary.title": "Бестиарий обновлён!",
  "hint.first_bestiary.body": "Первая запись в бестиарии добавлена. Изучай врагов на вкладке Бестиарий.",
  "hint.hunger_warning.title": "Внимание: голод!",
  "hint.hunger_warning.body": "Голод достигает критического уровня. Используй Поиск Еды, пока характеристики не упали.",
  "hint.goto": "Перейти",
  "guide.title": "Руководство",
  "guide.basics.title": "Шкалы состояния",
  "guide.basics.body": "HP (зелёная): здоровье — при 0 смерть. MP (синяя): мана, тратится на навыки. SP (золотая): выносливость, для особых способностей. Голод (коричневая): растёт со временем — высокий голод снижает характеристики.",
  "guide.combat.title": "Система боя",
  "guide.combat.body": "На вкладке Бой ты сражаешься с врагами. Авто-режим атакует каждую секунду. Ручной режим позволяет самому активировать навыки. Убитые враги дают XP (уровень) и опыт навыков. Задержка атаки определяет скорость ударов.",
  "guide.skills.title": "Навыки и Эволюция",
  "guide.skills.body": "Навыки получают опыт при использовании в бою. На 10-м уровне навык может эволюционировать; некоторые ветвятся — выбирай осторожно. Дерево навыков показывает цепочки и условия разблокировки. Навыки нужно экипировать (слоты ограничены).",
  "guide.map.title": "Карта и Комнаты",
  "guide.map.body": "Подземелье: Слой → Этаж → Комната (L.F.R вверху). Зачисти комнаты — победи босса — следующий этаж открыт. На вкладке Карта можно вернуться на пройденные этажи для фарма. Каждый слой требует определённого уровня.",
  "guide.hunger.title": "Голод и Еда",
  "guide.hunger.body": "Голод растёт во время боя и отдыха. При >70% характеристики снижаются. Кнопка Поиск Еды ищет пропитание. Трупы врагов можно хранить в кладовой и съесть позже. Авто-Еда ест автоматически при высоком голоде.",
  "guide.stats.title": "Статы и Очки",
  "guide.stats.body": "6 основных стата: STR (физическая атака), VIT (макс HP), AGI (скорость и уклонение), INT (макс MP и эффективность навыков), WIS (восстановление маны), LUCK (добыча и крит). На каждом уровне даются очки. Трать осторожно — рефанд стоит EP.",
  "guide.inventory.title": "Инвентарь и Снаряжение",
  "guide.inventory.body": "Доступно для человекоподобных рас. Найденные предметы попадают в инвентарь. Экипируй их для бонусов к статам. Forge повышает редкость (стоит EP). Утилизируй Common предметы для EP. Авто-экип надевает лучшее снаряжение автоматически.",
  "guide.soul.title": "Дерево Душ и Медитация",
  "guide.soul.body": "Души получаются от убийств (с высоким Грехом) и тратятся в Дереве Душ на постоянные улучшения — сохраняются после перерождения. Медитация увеличивает Добродетель и открывает светлые силы правителя. Всё это на вкладке Статы.",
  "guide.fusion.title": "Слияние навыков",
  "guide.fusion.body": "После разблокировки Слияния можно объединять два навыка в гибридный. Выбери два навыка на вкладке Навыки и нажми Слить. Результат определяется комбинацией — экспериментируй! Результаты слияния одинаковы для всех игроков.",
  "guide.human_path.title": "Путь Человека",
  "guide.human_path.body": "Если раса Human или достигла человекоподобной эволюции, открывается выбор специализации — Путь Человека. Это одноразовое решение. Выбирай мудро — отменить без перерождения нельзя.",
  "guide.lore.title": "Бестиарий и Лор",
  "guide.lore.body": "Бестиарий записывает каждого встреченного врага; после достаточного числа боёв показывает статы и слабости. Вкладка Лор содержит найденные в подземелье книги — чтение даёт EP и бонус INT. Лог Лора внизу справа хранит все записи книг.",
  "guide.races.title": "Расы",
  "guide.races.body": "Раса определяет начальные навыки, статы и дерево эволюции. Можно менять расу до первого убийства. У каждой расы свои сильные стороны и пути развития — смотри карточки ниже.",
  "race_hint.strength": "Сильная сторона",
  "race_hint.weakness": "Слабая сторона",
  "race_hint.growth": "Путь развития",
  "race_hint.note": "Примечание"
```

- [ ] **Step 5: Build + key count check**

```bash
cd D:\MonsterReincarnationIncremental && npm run build
node -e "const e=require('./data/i18n/en.json'); const r=require('./data/i18n/ru.json'); const t=require('./data/i18n/tr.json'); const ek=Object.keys(e); const rk=Object.keys(r); const tk=Object.keys(t); const missing_r=ek.filter(k=>!(k in r)); const missing_t=ek.filter(k=>!(k in t)); console.log('EN keys:', ek.length, '| RU missing:', missing_r.length, '| TR missing:', missing_t.length); if(missing_r.length) console.log('RU missing:', missing_r.slice(0,5)); if(missing_t.length) console.log('TR missing:', missing_t.slice(0,5));"
```

Beklenen: `RU missing: 0 | TR missing: 0`

- [ ] **Step 6: Commit**

```bash
git add data/i18n/en.json data/i18n/tr.json data/i18n/ru.json
git commit -m "feat: i18n — tutorial/guide/hint anahtarları (tr/en/ru)"
```

---

## Task 5: UiActions Extension + main.ts Handlers

**Files:**
- Modify: `packages/client/src/ui.ts` (sadece UiActions interface)
- Modify: `packages/client/src/main.ts`

**Interfaces:**
- Consumes: `GameState.tutorialStep`, `GameState.seenHints` (Task 1)
- Produces: `UiActions.onTutorialNext`, `onTutorialSkip`, `onTutorialReopen`, `onMarkHintSeen`, `onNavigateGuide`

- [ ] **Step 1: `ui.ts` — UiActions interface'e ekle**

`UiActions` interface'inde `onForage: () => void;` satırının altına ekle:

```typescript
  onTutorialNext: () => void;
  onTutorialSkip: () => void;
  onTutorialReopen: () => void;
  onMarkHintSeen: (id: string) => void;
  onNavigateGuide: (anchor: string) => void;
```

- [ ] **Step 2: `main.ts` — actions objesine handler'ları ekle**

`main.ts`'deki `actions` objesinde `onForage:` bloğunun hemen altına ekle:

```typescript
    onTutorialNext: () => {
      if (typeof state.tutorialStep === 'number') {
        const nextStep = state.tutorialStep + 1;
        state.tutorialStep = nextStep >= 8 ? 'done' : nextStep;
      }
      save(state);
      render(state);
    },
    onTutorialSkip: () => {
      state.tutorialStep = 'skipped';
      save(state);
      render(state);
    },
    onTutorialReopen: () => {
      state.tutorialStep = 0;
      save(state);
      render(state);
    },
    onMarkHintSeen: (id: string) => {
      if (!state.seenHints.includes(id)) state.seenHints.push(id);
      save(state);
    },
    onNavigateGuide: (anchor: string) => {
      // activeTab değişimi ve render ui.ts'den yapılır; burada sadece save.
      save(state);
    },
```

- [ ] **Step 3: Build kontrolü**

```bash
cd D:\MonsterReincarnationIncremental && npm run build
```

Beklenen: `✓ built in ...ms`

- [ ] **Step 4: Commit**

```bash
git add packages/client/src/ui.ts packages/client/src/main.ts
git commit -m "feat: UiActions + main.ts tutorial handler'ları"
```

---

## Task 6: Guide Sekmesi

**Files:**
- Modify: `packages/client/src/ui.ts`

**Interfaces:**
- Consumes: `t()`, `UiActions.onNavigateGuide` (Task 5), `race_hints.json` (Task 3)
- Produces: `guideTab(state): string`, `wireGuide(el)`, `'guide'` Tab tipi

- [ ] **Step 1: `ui.ts` — Tab tipine 'guide' ekle**

Dosyanın başındaki `type Tab = ...` satırını bul (satır ~80):

```typescript
type Tab = 'combat' | 'map' | 'skills' | 'body' | 'inventory' | 'lore' | 'bestiary' | 'stats' | 'settings';
const TABS: Tab[] = ['combat', 'map', 'skills', 'body', 'inventory', 'lore', 'bestiary', 'stats', 'settings'];
```

Şöyle değiştir:

```typescript
type Tab = 'combat' | 'map' | 'skills' | 'body' | 'inventory' | 'lore' | 'bestiary' | 'stats' | 'settings' | 'guide';
const TABS: Tab[] = ['combat', 'map', 'skills', 'body', 'inventory', 'lore', 'bestiary', 'stats', 'settings', 'guide'];
```

- [ ] **Step 2: `ui.ts` — ICONS objesine guide ikonunu ekle**

`ICONS` objesindeki `settings:` satırının sonuna (closing `}` öncesi) virgül + ekle:

```typescript
  guide: svg('<path d="M4 4h12a2 2 0 0 1 2 2v14H6a2 2 0 0 1-2-2z"/><path d="M8 8h8M8 12h8M8 16h4"/><circle cx="18" cy="18" r="3"/><path d="M18 16v2l1 1"/>'),
```

- [ ] **Step 3: `ui.ts` — renderTab() switch'e 'guide' ekle**

`renderTab()` fonksiyonundaki switch'te `case 'settings':` bloğundan sonra ekle:

```typescript
    case 'guide':
      el.innerHTML = guideTab(CURSTATE);
      wireGuide(el);
      break;
```

- [ ] **Step 4: `ui.ts` — guideTab() fonksiyonunu ekle**

`settingsTab()` fonksiyonundan önce `guideTab()` ve `wireGuide()` ekle:

```typescript
// ---- GUIDE TAB -------------------------------------------------------------

/** Harici race_hints.json verisi — ilk yüklemede fetch edilir, cache'lenir. */
let raceHintsCache: Record<string, Record<string, Record<string, string>>> | null = null;

async function loadRaceHints(base: string): Promise<void> {
  try {
    raceHintsCache = await fetch(`${base}race_hints.json`).then((r) => r.json());
  } catch {
    raceHintsCache = {};
  }
}

function raceHintPanel(raceId: string, lang: string): string {
  if (!raceHintsCache) return '';
  const hint = raceHintsCache[raceId];
  if (!hint) return '';
  const l = (hint[lang] ?? hint['en'] ?? {}) as Record<string, string>;
  return `
    <div class="race-hint-panel" style="margin-top:0.8rem; padding:0.8rem; background:rgba(255,255,255,0.04); border-radius:6px; font-size:0.85rem; line-height:1.5;">
      ${l.strength ? `<div><span style="color:var(--venom)">⬆ ${t('race_hint.strength')}:</span> ${l.strength}</div>` : ''}
      ${l.weakness ? `<div><span style="color:var(--blood)">⬇ ${t('race_hint.weakness')}:</span> ${l.weakness}</div>` : ''}
      ${l.growth ? `<div><span class="muted">🌱 ${t('race_hint.growth')}:</span> ${l.growth}</div>` : ''}
      ${l.note ? `<div><span class="muted">✦ ${t('race_hint.note')}:</span> ${l.note}</div>` : ''}
    </div>`;
}

const GUIDE_SECTIONS = [
  'basics', 'combat', 'skills', 'map', 'hunger', 'stats',
  'inventory', 'soul', 'fusion', 'human_path', 'lore', 'races',
] as const;

function guideSectionHtml(section: string, state: GameState): string {
  const isRaces = section === 'races';
  const racesExtra = isRaces
    ? [...(CONTENT?.races?.values() ?? [])].map((r) => {
        const hint = raceHintPanel(r.id, state.lang ?? 'en');
        return hint
          ? `<div style="margin-top:1rem;"><b>${t(r.locKey)}</b>${hint}</div>`
          : '';
      }).join('')
    : '';
  return `
    <section class="panel" id="guide-${section}" style="scroll-margin-top:1rem;">
      <h3 style="margin-bottom:0.5rem;">${t(`guide.${section}.title`)}</h3>
      <p class="muted" style="line-height:1.6;">${t(`guide.${section}.body`)}</p>
      ${racesExtra}
    </section>`;
}

function guideTab(state: GameState): string {
  const sections = GUIDE_SECTIONS.map((s) => guideSectionHtml(s, state)).join('');
  return `
    <div style="padding:0.5rem 0;">
      <h2 style="margin-bottom:1rem;">${t('guide.title')}</h2>
      ${sections}
    </div>`;
}

function wireGuide(_el: HTMLElement): void {
  // Guide sekmesi tamamen statik HTML — etkileşim yok.
}
```

- [ ] **Step 5: `ui.ts` — mount() içinde loadRaceHints çağır**

`mount()` fonksiyonunun başında `CONTENT = content;` satırının hemen ardına ekle:

```typescript
  void loadRaceHints(import.meta.env.BASE_URL);
```

- [ ] **Step 6: Build kontrolü**

```bash
cd D:\MonsterReincarnationIncremental && npm run build
```

Beklenen: `✓ built in ...ms`

- [ ] **Step 7: Manuel test**

`npm run dev` ile oyunu aç. Navigasyonda Kılavuz sekmesinin göründüğünü ve tüm 12 bölümün listelendiğini kontrol et.

- [ ] **Step 8: Commit**

```bash
git add packages/client/src/ui.ts
git commit -m "feat: Kılavuz sekmesi — 12 bölüm + race hint paneli"
```

---

## Task 7: Tutorial Sihirbazı Overlay

**Files:**
- Modify: `packages/client/src/ui.ts`
- Modify: `packages/client/index.html` (CSS)

**Interfaces:**
- Consumes: `TUTORIAL_STEPS`, `UiActions.onTutorialNext`, `onTutorialSkip`, `onTutorialReopen` (Task 5), `t()` (Task 4)
- Produces: `updateTutorialOverlay(state)`, `wireTutorialOverlay()`

- [ ] **Step 1: `index.html` — wizard CSS ekle**

`index.html` dosyasındaki `<style>` bloğunun sonuna (son `</style>` öncesi) ekle:

```css
.tutorial-overlay {
  position: fixed; inset: 0; z-index: 1000;
  background: rgba(0,0,0,0.55); display: flex;
  align-items: center; justify-content: center;
  padding: 1rem;
}
.tutorial-overlay.hidden { display: none; }
.tutorial-box {
  background: var(--panel, #1a1725); border: 1px solid rgba(255,255,255,0.1);
  border-radius: 10px; padding: 1.5rem; max-width: 480px; width: 100%;
  box-shadow: 0 8px 32px rgba(0,0,0,0.6);
}
.tutorial-box h2 { margin: 0 0 0.75rem; font-size: 1.1rem; color: var(--venom, #6fae53); }
.tutorial-box p { margin: 0 0 1.2rem; line-height: 1.6; color: rgba(255,255,255,0.85); font-size:0.92rem; }
.tutorial-step-count { font-size: 0.75rem; color: rgba(255,255,255,0.4); margin-bottom: 0.5rem; }
.tutorial-actions { display: flex; gap: 0.5rem; flex-wrap: wrap; justify-content: flex-end; }
.tutorial-goto { margin-right: auto; }
```

- [ ] **Step 2: `ui.ts` — tutorial import ekle**

Dosyanın başındaki import bloğuna (diğer import'ların altına) ekle:

```typescript
import { TUTORIAL_STEPS, type TutorialStep } from './tutorial';
```

- [ ] **Step 3: `ui.ts` — mount() içinde overlay div ekle**

`mount()` fonksiyonundaki `app.innerHTML = \`...\`` bloğunda `<div id="toasts"` satırının hemen altına (toasts div'inden sonra) ekle:

```typescript
      <div id="tutorial-overlay" class="tutorial-overlay hidden"></div>
```

- [ ] **Step 4: `ui.ts` — updateTutorialOverlay() fonksiyonunu ekle**

`guideTab()` fonksiyonundan önce ekle:

```typescript
// ---- TUTORIAL OVERLAY ------------------------------------------------------

function updateTutorialOverlay(state: GameState): void {
  const overlay = document.querySelector<HTMLElement>('#tutorial-overlay');
  if (!overlay) return;

  const step = state.tutorialStep;

  // Overlay'i gizle: tamamlandı / skip'lendi / raceConfirmed değil
  if (step === 'done' || step === 'skipped' || !state.raceConfirmed) {
    overlay.classList.add('hidden');
    return;
  }

  if (typeof step !== 'number') { overlay.classList.add('hidden'); return; }

  const def: TutorialStep | undefined = TUTORIAL_STEPS[step];
  if (!def) { overlay.classList.add('hidden'); return; }

  const isFirst = step === 0;
  const isReopen = state.tutorialStep === 0 && (state as any).__tutorialWasSkipped;
  const titleKey = isReopen ? 'tut.reopen_greeting' : `${def.i18nPrefix}.title`;
  const total = TUTORIAL_STEPS.length;
  const gotoBtn = def.targetTab
    ? `<button class="ghost tut-goto tutorial-goto" data-tab="${def.targetTab}" data-sel="${def.targetSelector ?? ''}">${t('tut.goto')}</button>`
    : '';
  const isLast = step === total - 1;

  overlay.innerHTML = `
    <div class="tutorial-box">
      <div class="tutorial-step-count">${t('tut.step')} ${step + 1} ${t('tut.of')} ${total}</div>
      <h2>${t(titleKey)}</h2>
      <p>${t(`${def.i18nPrefix}.body`)}</p>
      <div class="tutorial-actions">
        ${gotoBtn}
        <button class="ghost tut-skip">${t('tut.skip_all')}</button>
        <button class="actbtn active tut-next">${isLast ? t('tut.done') : t('tut.next')}</button>
      </div>
    </div>`;

  overlay.classList.remove('hidden');
  wireTutorialOverlay(overlay);
}

function wireTutorialOverlay(overlay: HTMLElement): void {
  overlay.querySelector<HTMLButtonElement>('.tut-next')?.addEventListener('click', () => {
    ACTIONS.onTutorialNext();
  });
  overlay.querySelector<HTMLButtonElement>('.tut-skip')?.addEventListener('click', () => {
    ACTIONS.onTutorialSkip();
  });
  overlay.querySelectorAll<HTMLButtonElement>('.tut-goto').forEach((btn) => {
    btn.addEventListener('click', () => {
      const tab = btn.getAttribute('data-tab') as Tab | null;
      const sel = btn.getAttribute('data-sel');
      if (tab) {
        activeTab = tab;
        renderTab();
        if (sel) {
          setTimeout(() => {
            const el = document.querySelector<HTMLElement>(sel);
            el?.scrollIntoView({ behavior: 'smooth', block: 'center' });
          }, 100);
        }
      }
    });
  });
}
```

- [ ] **Step 5: `ui.ts` — render() içinde updateTutorialOverlay çağır**

`render()` fonksiyonunu bul (export fonksiyonu). Mevcut içeriğin sonuna ekle:

```typescript
  updateTutorialOverlay(state);
```

`render()` fonksiyonu şu an muhtemelen şöyle görünüyor:
```typescript
export function render(state: GameState): void {
  CURSTATE = state;
  renderTab();
  updateTopbar(state);
}
```

Şöyle olmalı:
```typescript
export function render(state: GameState): void {
  CURSTATE = state;
  renderTab();
  updateTopbar(state);
  updateTutorialOverlay(state);
}
```

- [ ] **Step 6: `ui.ts` — settingsTab() içine "Tutorial'ı Aç" butonu ekle**

`settingsTab()` fonksiyonundaki `return` bloğunda ilk `<section class="panel">` açılışına (diffs/permadeath bölümü değil) `<section class="panel">` bloklarından birinin sonuna ekle. En alt `<section class="panel">` içindeki butonların olduğu `<div class="controls">` bloğuna:

```typescript
      <div class="controls">
        <button id="tutorial-reopen">${t('tut.reopen')}</button>
      </div>
```

- [ ] **Step 7: `ui.ts` — wireSettings() içine tutorial reopen handler ekle**

`wireSettings()` fonksiyonunun sonuna ekle:

```typescript
  el.querySelector<HTMLButtonElement>('#tutorial-reopen')?.addEventListener('click', ACTIONS.onTutorialReopen);
```

- [ ] **Step 8: Build + manuel test**

```bash
cd D:\MonsterReincarnationIncremental && npm run build
```

Yeni oyun başlatarak wizard'ın açıldığını, İleri/Atla butonlarının çalıştığını, Götür butonunun ilgili sekmeye gittiğini kontrol et. Ayarlar → "Tutorial'ı Aç" butonunu kontrol et.

- [ ] **Step 9: Commit**

```bash
git add packages/client/src/ui.ts packages/client/index.html
git commit -m "feat: tutorial sihirbazı overlay — 8 adım, skip/next/goto"
```

---

## Task 8: Hint Toast Sistemi

**Files:**
- Modify: `packages/client/src/ui.ts`

**Interfaces:**
- Consumes: `HINT_DEFS`, `shouldShowHint`, `markHintSeen` (Task 2), `UiActions.onMarkHintSeen`, `onNavigateGuide` (Task 5)
- Produces: `checkAndShowHints(state)` — render() içinde çağrılır

- [ ] **Step 1: `ui.ts` — hint import'u ekle**

Mevcut tutorial import satırını güncelle:

```typescript
import { TUTORIAL_STEPS, HINT_DEFS, shouldShowHint, markHintSeen, type TutorialStep } from './tutorial';
```

- [ ] **Step 2: `ui.ts` — pushHintToast() fonksiyonunu ekle**

Mevcut `pushToast()` fonksiyonunun hemen altına ekle:

```typescript
/** Tutorial hint toast'u — "Götür" linki ile. Hint ID'sini seenHints'e ekler. */
function pushHintToast(hintId: string, targetTab: string | null, guideAnchor: string): void {
  const host = document.querySelector<HTMLElement>('#toasts');
  if (!host) return;
  const el = document.createElement('div');
  el.className = 'toast';
  el.style.cssText = 'cursor:default; padding: 0.6rem 0.8rem;';

  const titleKey = `hint.${hintId}.title`;
  const bodyKey = `hint.${hintId}.body`;
  const gotoHtml = targetTab
    ? `<button class="ghost hint-goto" data-tab="${targetTab}" data-anchor="${guideAnchor}" style="margin-top:0.4rem;font-size:0.78rem;padding:0.2rem 0.6rem;">${t('hint.goto')}</button>`
    : '';

  el.innerHTML = `
    <div style="font-weight:600;font-size:0.88rem;margin-bottom:0.2rem;">${t(titleKey)}</div>
    <div style="font-size:0.82rem;opacity:0.85;">${t(bodyKey)}</div>
    ${gotoHtml}`;

  const dismiss = (): void => {
    el.classList.add('out');
    setTimeout(() => el.remove(), 350);
  };
  el.querySelector('.hint-goto')?.addEventListener('click', (e) => {
    e.stopPropagation();
    const tab = (e.currentTarget as HTMLElement).getAttribute('data-tab') as Tab | null;
    const anchor = (e.currentTarget as HTMLElement).getAttribute('data-anchor') ?? '';
    if (tab) {
      activeTab = tab;
      renderTab();
      setTimeout(() => {
        const target = document.querySelector<HTMLElement>(`#${anchor}`);
        target?.scrollIntoView({ behavior: 'smooth', block: 'start' });
      }, 100);
    }
    dismiss();
  });
  el.addEventListener('click', dismiss);
  host.appendChild(el);
  while (host.children.length > 6 && host.firstChild) host.removeChild(host.firstChild);
  setTimeout(dismiss, 12000);
}
```

- [ ] **Step 3: `ui.ts` — checkAndShowHints() fonksiyonunu ekle**

`pushHintToast()` fonksiyonunun hemen altına ekle:

```typescript
/** Tetiklenmesi gereken hint'leri kontrol eder, gösterir ve seenHints'e ekler. */
function checkAndShowHints(state: GameState): void {
  for (const def of HINT_DEFS) {
    if (!shouldShowHint(def.id, state)) continue;
    markHintSeen(def.id, state);
    ACTIONS.onMarkHintSeen(def.id);
    pushHintToast(def.id, def.targetTab, def.guideAnchor);
  }
}
```

- [ ] **Step 4: `ui.ts` — render() içine checkAndShowHints ekle**

`render()` fonksiyonuna `updateTutorialOverlay(state);` satırından önce:

```typescript
  checkAndShowHints(state);
```

Sonuçta `render()`:
```typescript
export function render(state: GameState): void {
  CURSTATE = state;
  renderTab();
  updateTopbar(state);
  checkAndShowHints(state);
  updateTutorialOverlay(state);
}
```

- [ ] **Step 5: Build + manuel test**

```bash
cd D:\MonsterReincarnationIncremental && npm run build
```

Oyunda bir skill'i 10. seviyeye çıkar — `hint.evo_available` toast'unun çıktığını ve "Götür" butonunun Stats sekmesine gittiğini doğrula. Aynı hint'in bir daha çıkmadığını kontrol et.

- [ ] **Step 6: Commit**

```bash
git add packages/client/src/ui.ts
git commit -m "feat: hint toast sistemi — 10 bağlamsal bildirim + navigate"
```

---

## Task 9: ? İkonları + Race Seçim Hint Paneli

**Files:**
- Modify: `packages/client/src/ui.ts`
- Modify: `packages/client/index.html` (CSS)

**Interfaces:**
- Consumes: `guideTab` anchor sistemi (Task 6), `raceHintPanel()` (Task 6)

- [ ] **Step 1: `index.html` — ? ikon CSS ekle**

`<style>` bloğuna ekle:

```css
.guide-link {
  display: inline-flex; align-items: center; justify-content: center;
  width: 18px; height: 18px; border-radius: 50%;
  background: rgba(255,255,255,0.08); color: rgba(255,255,255,0.5);
  font-size: 0.7rem; cursor: pointer; border: 1px solid rgba(255,255,255,0.12);
  margin-left: 0.3rem; vertical-align: middle; flex-shrink: 0;
  transition: background 0.15s;
}
.guide-link:hover { background: rgba(255,255,255,0.18); color: #fff; }
```

- [ ] **Step 2: `ui.ts` — guideLink() yardımcı fonksiyonu ekle**

`guideTab()` fonksiyonunun hemen üstüne ekle:

```typescript
/** Kılavuz sekmesinin belirtilen anchor'ına navigate eden küçük ? butonu. */
function guideLink(anchor: string): string {
  return `<button class="guide-link" data-guide="${anchor}" title="${t('guide.title')}">?</button>`;
}
```

- [ ] **Step 3: `ui.ts` — guide link click'lerini document level'da yakala**

`mount()` fonksiyonunun sonuna (event listener bloklarının altına) ekle:

```typescript
  document.addEventListener('click', (e) => {
    const btn = (e.target as HTMLElement).closest<HTMLButtonElement>('.guide-link');
    if (!btn) return;
    const anchor = btn.getAttribute('data-guide');
    if (!anchor) return;
    activeTab = 'guide';
    renderTab();
    setTimeout(() => {
      const el = document.querySelector<HTMLElement>(`#${anchor}`);
      el?.scrollIntoView({ behavior: 'smooth', block: 'start' });
    }, 80);
  });
```

- [ ] **Step 4: `ui.ts` — topbar'a ? ikonları ekle**

`topbarHtml()` fonksiyonunda HP/MP/SP bar satırlarını güncelle. Şu an:

```typescript
      ${statBarSkel('tb-hp', t('ui.hp'), '#6fae53')}
      ${statBarSkel('tb-mp', t('ui.mp'), '#4f86c2')}
      ${statBarSkel('tb-sp', t('ui.sp'), '#d2a73a')}
      ${statBarSkel('tb-hunger', t('ui.hunger'), HUNGER_COLORS[0])}
```

`tb-hp` satırından önce başlık satırı ekle:

```typescript
      <div style="display:flex;align-items:center;gap:0.3rem;margin-bottom:0.2rem;font-size:0.78rem;color:rgba(255,255,255,0.4);">
        ${t('ui.hp')}/${t('ui.mp')}/${t('ui.sp')} ${guideLink('guide-basics')}
      </div>
      ${statBarSkel('tb-hp', t('ui.hp'), '#6fae53')}
      ${statBarSkel('tb-mp', t('ui.mp'), '#4f86c2')}
      ${statBarSkel('tb-sp', t('ui.sp'), '#d2a73a')}
      ${statBarSkel('tb-hunger', t('ui.hunger'), HUNGER_COLORS[0])}
```

- [ ] **Step 5: `ui.ts` — sekme başlıklarına ? ikonları ekle**

`mount()` içindeki sekme butonlarını oluşturan satır:

```typescript
${TABS.map((tb) => `<button class="tabbtn" data-tab="${tb}">${ICONS[tb]}<span>${t(`tab.${tb}`)}</span></button>`).join('')}
```

Güncelle:

```typescript
${TABS.map((tb) => {
  const anchorMap: Partial<Record<Tab, string>> = {
    combat: 'guide-combat', map: 'guide-map', skills: 'guide-skills',
    inventory: 'guide-inventory', lore: 'guide-lore', bestiary: 'guide-lore',
    stats: 'guide-stats',
  };
  const anchor = anchorMap[tb];
  const helpBtn = anchor ? guideLink(anchor) : '';
  return `<button class="tabbtn" data-tab="${tb}">${ICONS[tb]}<span>${t(`tab.${tb}`)}</span>${helpBtn}</button>`;
}).join('')}
```

- [ ] **Step 6: `ui.ts` — raceSelectScreen() içinde race hint paneli ekle**

`raceSelectScreen()` fonksiyonundaki `raceCards` map bloğunda her `<button class="race-card"...>` içindeki `${startSkillNames ? ...}` satırının hemen altına ekle:

```typescript
          ${raceHintPanel(r.id, state.lang ?? 'en')}
```

- [ ] **Step 7: Build + manuel test**

```bash
cd D:\MonsterReincarnationIncremental && npm run build
```

Kontrol listesi:
- [ ] Topbar'da HP/MP/SP başlığı yanında `?` butonu görünüyor
- [ ] Sekme butonlarının yanında `?` butonları görünüyor  
- [ ] `?` butonlarına tıklanınca Kılavuz sekmesi açılıyor ve ilgili bölüme scroll yapıyor
- [ ] Race seçim ekranında örümcek ırkının altında hint paneli görünüyor (güçlü/zayıf yön vb.)

- [ ] **Step 8: Version bump ve changelog**

`packages/client/src/changelog.ts` dosyasında VERSION'u güncelle ve yeni giriş ekle:

```typescript
export const VERSION = '2.0.0';
```

CHANGELOG array başına ekle:

```typescript
{
  version: '2.0.0',
  tr: [
    'Tutorial sistemi: 8 adımlı sihirbaz, 10 hint bildirimi, Kılavuz sekmesi',
    'Race seçim ekranına ırk açıklaması paneli eklendi',
    '? ikonları: her sekmeye ve topbar\'a Kılavuz navigasyonu',
    'Ayarlar\'dan tutorial sıfırlanabilir',
  ],
  en: [
    'Tutorial system: 8-step wizard, 10 hint toasts, Guide tab',
    'Race selection now shows race hint panel (strengths, growth path)',
    '? icons on tabs and topbar navigate to Guide sections',
    'Tutorial can be reopened from Settings',
  ],
  ru: [
    'Система туториала: мастер из 8 шагов, 10 подсказок, вкладка Руководство',
    'Экран выбора расы теперь показывает панель подсказок расы',
    'Иконки ? на вкладках и панели навигируют в Руководство',
    'Туториал можно снова открыть из Настроек',
  ],
},
```

- [ ] **Step 9: Final build + push**

```bash
cd D:\MonsterReincarnationIncremental && npm run build
git add packages/client/src/ui.ts packages/client/index.html packages/client/src/changelog.ts
git commit -m "feat: ? ikonları + race hint paneli + v2.0.0 changelog"
git push
```

---

## Self-Review

**Spec coverage:**
- ✅ 8 adımlı sihirbaz (Task 7)
- ✅ 10 contextual hint (Task 8)
- ✅ Kılavuz sekmesi 12 bölüm (Task 6)
- ✅ ? ikonları + navigate (Task 9)
- ✅ Race hint paneli (Task 3, 6, 9)
- ✅ Skip / reopen / "bak kim gelmiş" (Task 7)
- ✅ Ayarlar'dan reopen butonu (Task 7)
- ✅ Tüm metin i18n (Task 4)
- ✅ State + migration (Task 1)
- ✅ TR/EN/RU (Task 4)

**Type consistency:**
- `TutorialStep.targetTab: string | null` → Task 7'de `as Tab | null` cast var ✅
- `UiActions.onMarkHintSeen(id: string)` → Task 5 ve Task 8 tutarlı ✅
- `raceHintPanel()` Task 6'da tanımlanıyor, Task 9'da aynı isimle kullanılıyor ✅
- `guideLink()` Task 9'da tanımlanıyor, aynı task içinde kullanılıyor ✅
- `HINT_DEFS[n].trigger` `(state: GameState) => boolean` — Task 2'de tanım, Task 8'de `shouldShowHint` üzerinden çağrı ✅
